// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: emojis.sql

package repo

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const createEmoji = `-- name: CreateEmoji :one
INSERT INTO
    emojis (
        server_id,
        name,
        image_url,
        creator_id,
        require_colons,
        animated
    )
VALUES ($1, $2, $3, $4, $5, $6)
RETURNING
    id, server_id, name, image_url, creator_id, require_colons, managed, animated, available, is_deleted, created_at
`

type CreateEmojiParams struct {
	ServerID      int32       `json:"server_id"`
	Name          string      `json:"name"`
	ImageUrl      string      `json:"image_url"`
	CreatorID     pgtype.Int4 `json:"creator_id"`
	RequireColons pgtype.Bool `json:"require_colons"`
	Animated      pgtype.Bool `json:"animated"`
}

func (q *Queries) CreateEmoji(ctx context.Context, arg CreateEmojiParams) (Emoji, error) {
	row := q.db.QueryRow(ctx, createEmoji,
		arg.ServerID,
		arg.Name,
		arg.ImageUrl,
		arg.CreatorID,
		arg.RequireColons,
		arg.Animated,
	)
	var i Emoji
	err := row.Scan(
		&i.ID,
		&i.ServerID,
		&i.Name,
		&i.ImageUrl,
		&i.CreatorID,
		&i.RequireColons,
		&i.Managed,
		&i.Animated,
		&i.Available,
		&i.IsDeleted,
		&i.CreatedAt,
	)
	return i, err
}

const getEmojiByID = `-- name: GetEmojiByID :one
SELECT id, server_id, name, image_url, creator_id, require_colons, managed, animated, available, is_deleted, created_at FROM emojis WHERE id = $1 AND is_deleted = FALSE LIMIT 1
`

func (q *Queries) GetEmojiByID(ctx context.Context, id int32) (Emoji, error) {
	row := q.db.QueryRow(ctx, getEmojiByID, id)
	var i Emoji
	err := row.Scan(
		&i.ID,
		&i.ServerID,
		&i.Name,
		&i.ImageUrl,
		&i.CreatorID,
		&i.RequireColons,
		&i.Managed,
		&i.Animated,
		&i.Available,
		&i.IsDeleted,
		&i.CreatedAt,
	)
	return i, err
}

const getServerEmojis = `-- name: GetServerEmojis :many
SELECT id, server_id, name, image_url, creator_id, require_colons, managed, animated, available, is_deleted, created_at
FROM emojis
WHERE
    server_id = $1
    AND available = TRUE
    AND is_deleted = FALSE
ORDER BY name
`

func (q *Queries) GetServerEmojis(ctx context.Context, serverID int32) ([]Emoji, error) {
	rows, err := q.db.Query(ctx, getServerEmojis, serverID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []Emoji
	for rows.Next() {
		var i Emoji
		if err := rows.Scan(
			&i.ID,
			&i.ServerID,
			&i.Name,
			&i.ImageUrl,
			&i.CreatorID,
			&i.RequireColons,
			&i.Managed,
			&i.Animated,
			&i.Available,
			&i.IsDeleted,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const hardDeleteEmoji = `-- name: HardDeleteEmoji :one
DELETE FROM emojis WHERE id = $1 RETURNING id, server_id, name, image_url, creator_id, require_colons, managed, animated, available, is_deleted, created_at
`

func (q *Queries) HardDeleteEmoji(ctx context.Context, id int32) (Emoji, error) {
	row := q.db.QueryRow(ctx, hardDeleteEmoji, id)
	var i Emoji
	err := row.Scan(
		&i.ID,
		&i.ServerID,
		&i.Name,
		&i.ImageUrl,
		&i.CreatorID,
		&i.RequireColons,
		&i.Managed,
		&i.Animated,
		&i.Available,
		&i.IsDeleted,
		&i.CreatedAt,
	)
	return i, err
}

const restoreEmoji = `-- name: RestoreEmoji :one
UPDATE emojis
SET
    is_deleted = FALSE,
    updated_at = CURRENT_TIMESTAMP
WHERE
    id = $1
RETURNING
    id, server_id, name, image_url, creator_id, require_colons, managed, animated, available, is_deleted, created_at
`

func (q *Queries) RestoreEmoji(ctx context.Context, id int32) (Emoji, error) {
	row := q.db.QueryRow(ctx, restoreEmoji, id)
	var i Emoji
	err := row.Scan(
		&i.ID,
		&i.ServerID,
		&i.Name,
		&i.ImageUrl,
		&i.CreatorID,
		&i.RequireColons,
		&i.Managed,
		&i.Animated,
		&i.Available,
		&i.IsDeleted,
		&i.CreatedAt,
	)
	return i, err
}

const searchEmojis = `-- name: SearchEmojis :many
SELECT id, server_id, name, image_url, creator_id, require_colons, managed, animated, available, is_deleted, created_at
FROM emojis
WHERE
    server_id = $1
    AND name ILIKE '%' || $2 || '%'
    AND available = TRUE
    AND is_deleted = FALSE
ORDER BY name
LIMIT $3
`

type SearchEmojisParams struct {
	ServerID int32       `json:"server_id"`
	Column2  pgtype.Text `json:"column_2"`
	Limit    int32       `json:"limit"`
}

func (q *Queries) SearchEmojis(ctx context.Context, arg SearchEmojisParams) ([]Emoji, error) {
	rows, err := q.db.Query(ctx, searchEmojis, arg.ServerID, arg.Column2, arg.Limit)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []Emoji
	for rows.Next() {
		var i Emoji
		if err := rows.Scan(
			&i.ID,
			&i.ServerID,
			&i.Name,
			&i.ImageUrl,
			&i.CreatorID,
			&i.RequireColons,
			&i.Managed,
			&i.Animated,
			&i.Available,
			&i.IsDeleted,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const softDeleteEmoji = `-- name: SoftDeleteEmoji :one
UPDATE emojis
SET
    is_deleted = TRUE,
    updated_at = CURRENT_TIMESTAMP
WHERE
    id = $1
RETURNING
    id, server_id, name, image_url, creator_id, require_colons, managed, animated, available, is_deleted, created_at
`

func (q *Queries) SoftDeleteEmoji(ctx context.Context, id int32) (Emoji, error) {
	row := q.db.QueryRow(ctx, softDeleteEmoji, id)
	var i Emoji
	err := row.Scan(
		&i.ID,
		&i.ServerID,
		&i.Name,
		&i.ImageUrl,
		&i.CreatorID,
		&i.RequireColons,
		&i.Managed,
		&i.Animated,
		&i.Available,
		&i.IsDeleted,
		&i.CreatedAt,
	)
	return i, err
}

const updateEmoji = `-- name: UpdateEmoji :one
UPDATE emojis
SET
    name = COALESCE($1, name),
    available = COALESCE(
        $2,
        available
    ),
    updated_at = CURRENT_TIMESTAMP
WHERE
    id = $3
    AND is_deleted = FALSE
RETURNING
    id, server_id, name, image_url, creator_id, require_colons, managed, animated, available, is_deleted, created_at
`

type UpdateEmojiParams struct {
	Name      pgtype.Text `json:"name"`
	Available pgtype.Bool `json:"available"`
	ID        int32       `json:"id"`
}

func (q *Queries) UpdateEmoji(ctx context.Context, arg UpdateEmojiParams) (Emoji, error) {
	row := q.db.QueryRow(ctx, updateEmoji, arg.Name, arg.Available, arg.ID)
	var i Emoji
	err := row.Scan(
		&i.ID,
		&i.ServerID,
		&i.Name,
		&i.ImageUrl,
		&i.CreatorID,
		&i.RequireColons,
		&i.Managed,
		&i.Animated,
		&i.Available,
		&i.IsDeleted,
		&i.CreatedAt,
	)
	return i, err
}
